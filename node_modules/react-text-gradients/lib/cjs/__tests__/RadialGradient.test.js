"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const React = require("react");
require("@testing-library/jest-dom");
const react_1 = require("@testing-library/react");
const components_1 = require("../components");
const RadialGradientTest = () => (React.createElement(components_1.RadialGradient, { fallbackColor: "red", className: "linear-gradient-text", gradient: ['#fff, #000'] }, "Radial Gradient Text"));
describe('RadialGradient', () => {
    test('renders correctly', () => {
        (0, react_1.render)(React.createElement(RadialGradientTest, null));
        expect(react_1.screen.getByText('Radial Gradient Text')).toBeInTheDocument();
    });
    test('should have background-image', () => {
        const { container } = (0, react_1.render)(React.createElement(RadialGradientTest, null));
        // NOTE: There are problems with `toHaveStyle` to be able to check the precise style of an property. And it only really checks if the style property exists.
        expect(container.firstChild).toHaveStyle('background-image: radial-gradient(#fff, #000)');
    });
});
//# sourceMappingURL=RadialGradient.test.js.map