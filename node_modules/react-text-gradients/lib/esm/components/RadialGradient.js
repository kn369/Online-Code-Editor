import * as React from 'react';
import { toGradientStyle } from '../helpers';
import { Span } from './Span';
export const RadialGradient = ({ children, gradient, fallbackColor, ...rest }) => {
    const style = {
        ...toGradientStyle(gradient, 'radial', fallbackColor),
        ...rest.style,
    };
    return (React.createElement(Span, { ...rest, style: style }, children));
};
//# sourceMappingURL=RadialGradient.js.map