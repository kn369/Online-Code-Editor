import React from 'react';
export declare type Gradient = {
    type?: 'linear' | 'radial';
    from?: string;
    to?: string;
    degree?: number;
};
export interface Props extends React.HTMLAttributes<HTMLDivElement> {
    gradient: Gradient;
    animate?: boolean;
    animateTo?: Gradient;
    animateDuration?: number;
}
export default function Text({ gradient, animate, animateTo, animateDuration, ...props }: Props): JSX.Element;
