import React from 'react';

declare type Gradient = {
    type?: 'linear' | 'radial';
    from?: string;
    to?: string;
    degree?: number;
};
interface Props extends React.HTMLAttributes<HTMLDivElement> {
    gradient: Gradient;
    animate?: boolean;
    animateTo?: Gradient;
    animateDuration?: number;
}
declare function Text({ gradient, animate, animateTo, animateDuration, ...props }: Props): JSX.Element;

export { Text as default };
